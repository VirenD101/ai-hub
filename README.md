# AI Hub 🚀
A side-by-side AI comparison dashboard built with Next.js 15 and AI SDK 6.

### Setup
1. Clone the repo
2. Run `npm install`
3. Rename `.env.example` to `.env` and add your keys.
4. Run `npm run dev`